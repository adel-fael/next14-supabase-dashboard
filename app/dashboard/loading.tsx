import { SpinnerLoader } from '@/components/Loaders';

const Loading = () => {
  return (
    <div className={'flex justify-center'}>
      <SpinnerLoader />
    </div>
  );
};

export default Loading;
